# Version control
.git
.gitignore
.distignore
.gitattributes

# macOS
.DS_Store
._*

# Windows
Thumbs.db
desktop.ini

# Development files (not needed in distribution)
node_modules
.phpcs.xml
phpcs.xml.dist
.eslintrc
composer.lock
composer.phar
package.json
package-lock.json

# Build tooling (users don't need these)
/vendor/
scoper.inc.php
build-scoped.sh
package-for-wporg.sh
build/composer.json
/bin/

# Build artifacts and staging
dist/
*.zip

# Documentation (all markdown files are GitHub only - readme.txt is for users)
*.md
**/*.md

# Examples (developer documentation only)
examples/

# CI/CD
.github
.claude

# Tests
tests
phpunit.xml
.phpunit.result.cache

# Screenshots and assets for wp.org
# These are usually uploaded separately to the assets folder
# Exclude all images EXCEPT icon-256x256.png (used in admin UI)
banner-*.png
screenshot-*.png
icon-128x128.png
*.jpg
*.gif
